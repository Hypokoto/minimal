# DefSec: Telemetry

- **System Logs**: `journalctl -p warning..alert`
- **Network Flows**: Monitor live connections with `sniffnet` or `bandwhich`.
- **Audit Framework**: Configure `auditd` to track specific file modifications and execution paths.
